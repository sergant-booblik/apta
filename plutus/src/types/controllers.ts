export enum Button {
  BUTTON = 'button',
  SUBMIT = 'submit',
  RESET = 'reset',
}

export enum Input {
  TEXT = 'text',
  EMAIL = 'email',
  PASSWORD = 'password',
}

export type ButtonType = Button;
export type InputType = Input;
